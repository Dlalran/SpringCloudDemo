server:
  port: 8084

spring:
  application:
    name: FeignConsumer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

#开启Hystrix支持
feign:
  hystrix:
    enabled: true
